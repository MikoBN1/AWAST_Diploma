services:
  zap:
    build: .
    image: my-zap:latest
    container_name: zap
    ports:
      - "8080:8080"
    volumes:
      - ./zap:/zap/wrk   # опционально - для сессий/репортов
    restart: unless-stopped
